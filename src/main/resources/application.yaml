spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/pdbdata
    username: postgres
    password: lemmer
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
